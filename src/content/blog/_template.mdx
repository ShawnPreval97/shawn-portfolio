{/*
  BLOG POST TEMPLATE
  ------------------
  This file is a draft template only. It uses published: false and a filename
  that starts with "_", so it never appears on the live website.

  How to publish a new blog post:

  1. Duplicate this file.
  2. Rename the copy using the post slug, for example:
     my-first-post.mdx
  3. Complete the metadata below (title, slug, date, excerpt, tags).
  4. Replace the sample Markdown body with your article.
  5. Change published to true when the post is ready.
  6. Preview locally with: npm run dev
  7. Confirm the production build with: npm run build
  8. Commit and deploy through the existing Git / Vercel workflow.

  To unpublish later, set published: false (or delete the file) and rebuild.
*/}

export const meta = {
  title: "Draft post title",
  slug: "draft-post-slug",
  date: "YYYY-MM-DD",
  excerpt: "Short homepage description for this draft.",
  tags: ["Draft"],
  published: false,
};

## Heading example

Replace this template body with your own writing. Keep the tone personal and
clear. This sample text is structural only and is not attributed as a finished
essay.

Use **bold** or *emphasis* when useful. Link out carefully, for example to the
[MDX documentation](https://mdxjs.com/).

### Smaller heading

- First list item
- Second list item
- Third list item

1. Numbered step one
2. Numbered step two
3. Numbered step three

> A blockquote can set off a short aside or quoted line without turning the
> post into formal research.

When you are finished drafting, set `published` to `true` in the metadata
export at the top of this file.
